# H — Workflow / output / analytics / i18n / issue trackers verification

Local finding: RED has NO merge/split (ROADMAP item 7 open). `gh search code '"Copy as RSpec"'` across GitHub → only AnjanJ/rails_error_dashboard.

H1 Copy as curl — SHARED-WITH Sentry (Request section curl toggle in every event: static/app/components/events/interfaces/request/index.tsx). Copy as RSpec — UNIQUE (nobody generates a runnable test). ROADMAP E "No competitor does this" wrong for curl.
H2 Copy for LLM — SHARED-WITH Better Stack ("AI prompt tab… ready-made prompt for Claude Code or Cursor with all necessary context", editable template w/ variables sidebar; betterstack.com/docs/errors/using-the-product/fix-with-ai/); Honeybadger partial (Markdown export: summary, trace, env, breadcrumbs). Self-hosted field converged on MCP instead (Faultline, Telebugs, GlitchTip). RED's bundle includes locals/ivars + system health — none of the others list that. UNIQUE-AMONG-SELF-HOSTED.
H3 AI Help BYO key in page — SHARED-WITH Raygun AI Error Resolution (OpenAI/Azure key, conversation history; no Anthropic). Sentry Seer hosted, no BYOK. UNIQUE-AMONG-SELF-HOSTED.
H4 Issue trackers — Codeberg UNIQUE (not found anywhere incl. Errbit plugins). Two-way sync SHARED-WITH Sentry (GitHub: resolve↔close), Raygun (Jira two-way), Rollbar (Jira/Shortcut sync). Honeybadger Linear one-direction state mapping. AppSignal per-user one-way. Scout GitHub/Linear one-way. Faultline GitHub one-way. Errbit GitHub + plugin gems. UNIQUE-AMONG-SELF-HOSTED for two-way sync, inbound webhooks, Linear, storm caps on auto-create. Sentry auto-create is Business-plan gated; no competitor mentions a rate cap on auto-created issues.
H5 i18n — SHARED-WITH Sentry (14 selectable UI languages, 45 locale dirs via Transifex), Errbit (en, pt-BR), GlitchTip (fr, nb). Honeybadger/AppSignal/Rollbar/Raygun/Scout/RorVsWild/Better Stack none found; NR/Datadog unverified. RED = most among self-hosted Rails tools and the only one localising emails/notification payloads (no competitor page mentions that).
H6 Digests — SHARED-WITH Sentry (weekly), Honeybadger (daily/weekly), Raygun (24h digest); UNIQUE-AMONG-SELF-HOSTED. Cooldown/thresholds/mute/snooze — TABLE-STAKES (Sentry rule frequency + "seen more than N times"; AppSignal warm-up/cooldown + Nth per hour/day; Raygun smart notifications; Telebugs cooldown+snooze 1h–3d; Bugsink mute + volume-based unmute; exception_notification log2 grouping; **Faultline `on_threshold: [10,50,100,500]` near-clone of RED's milestones + 5-min cooldown**).
H7 Analytics — Baseline anomaly: SHARED-WITH Sentry dynamic thresholds (metric alerts, not per-issue); AppSignal static only. Spike: SHARED-WITH Telebugs ("Spikes" rule), Sentry priority escalation on surge. Priority scoring: SHARED-WITH Sentry (auto priority, Business). Similar errors: SHARED-WITH Sentry Similar Issues, Honeybadger/Telebugs manual merge. User impact: Sentry counts users. **Cascades/correlation, occurrence patterns (time-of-day/day-of-week), platform comparison: UNIQUE (not found; Datadog/NR unverified).** All H7 UNIQUE-AMONG-SELF-HOSTED except spike (Telebugs).
H8 Workflow — TABLE-STAKES. Statuses/assign/comments/batch/auto-reopen/custom fingerprint standard in SaaS (Sentry, Honeybadger up to 10,000 batch, NR, Raygun, Scout, Telebugs). Among self-hosted Rails, assignment+comments+priority together = only RED and Telebugs; Faultline has bulk + auto-reopen + fingerprint lambda, no assign/comments; solid_errors resolve only. **RED lacks merge (Sentry, Honeybadger, Raygun, Telebugs have it).**
H9 Source — inline viewer SHARED-WITH Faultline ("side-by-side source and locals like a real debugger") + all SaaS SDKs (Sentry context_lines 3, Honeybadger source_radius 2, Bugsnag send_code, Airbrake code_hunks, Rollbar opt-in). **Local git blame with no repo OAuth = UNIQUE** (Rollbar/Sentry blame/suspect commits only via GitHub integration; AppSignal links only). SaaS snapshot source at capture; RED reads at display from live tree.
H10 — TABLE-STAKES (Sentry & Telebugs dark mode + shortcuts; Telebugs command palette ahead of RED's `?`).
Unverified (unreachable): Bugsnag, Airbrake, New Relic, Datadog product UI; Rollbar behind Cloudflare.
